<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mineboat - Panel Admin</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #00BFFF 0%, #87CEEB 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        .login-box {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            text-align: center;
        }

        .login-box h1 {
            color: #00BFFF;
            margin-bottom: 30px;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-group label {
            display: block;
            color: #333;
            margin-bottom: 8px;
            font-weight: bold;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 2px solid #87CEEB;
            border-radius: 8px;
            font-size: 16px;
            font-family: inherit;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #00BFFF;
        }

        textarea {
            min-height: 100px;
            resize: vertical;
        }

        button {
            padding: 12px 30px;
            background: linear-gradient(145deg, #00BFFF, #1E90FF);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 191, 255, 0.4);
        }

        .error {
            color: red;
            margin-top: 10px;
            display: none;
        }

        .success {
            color: green;
            margin-top: 10px;
            display: none;
        }

        .hidden {
            display: none;
        }

        .admin-section {
            background: white;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            margin-bottom: 20px;
        }

        .admin-section h2 {
            color: #00BFFF;
            margin-bottom: 20px;
        }

        .logout-btn {
            background: #ff4444;
            margin-bottom: 20px;
        }

        .logout-btn:hover {
            background: #cc0000;
        }

        .announcements-list {
            margin-top: 20px;
            max-height: 300px;
            overflow-y: auto;
        }

        .announcement-item {
            background: #f0f8ff;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 10px;
            border-left: 4px solid #00BFFF;
            position: relative;
        }

        .announcement-date {
            color: #666;
            font-size: 12px;
            margin-top: 5px;
        }

        .delete-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            background: #ff4444;
            padding: 5px 10px;
            font-size: 12px;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .stat-box {
            background: #f0f8ff;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }

        .stat-box h3 {
            color: #00BFFF;
            font-size: 32px;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- LOGIN -->
        <div id="loginSection" class="login-box">
            <h1>🔐 Panel de Administración</h1>
            <h2>MINEBOAT</h2>
            
            <div class="form-group">
                <label>Usuario</label>
                <input type="text" id="loginUser" placeholder="mineboat">
            </div>
            
            <div class="form-group">
                <label>Contraseña</label>
                <input type="password" id="loginPass" placeholder="Ingresa tu contraseña">
            </div>
            
            <button onclick="doLogin()">Iniciar Sesión</button>
            <p class="error" id="loginError">❌ Credenciales incorrectas</p>
        </div>

        <!-- ADMIN PANEL -->
        <div id="adminSection" class="hidden">
            <button class="logout-btn" onclick="doLogout()">Cerrar Sesión</button>
            
            <div class="admin-section">
                <h1>Bienvenido, Administrador 🚢</h1>
                <p>Panel de control de Mineboat</p>
            </div>

            <!-- ANUNCIOS -->
            <div class="admin-section">
                <h2>📢 Publicar Anuncio en la Página</h2>
                <div class="form-group">
                    <label>Mensaje del anuncio</label>
                    <textarea id="announcementText" placeholder="Escribe el anuncio que aparecerá en la página principal..."></textarea>
                </div>
                <button onclick="publishAnnouncement()">Publicar Anuncio</button>
                <p class="success" id="announcementSuccess">✅ Anuncio publicado exitosamente</p>
                
                <div class="announcements-list">
                    <h3 style="color: #00BFFF;">Anuncios Activos:</h3>
                    <div id="announcementsList"></div>
                </div>
            </div>

            <!-- DISCORD -->
            <div class="admin-section">
                <h2>💬 Enviar Mensaje a Discord</h2>
                <div class="form-group">
                    <label>Mensaje</label>
                    <textarea id="discordText" placeholder="Mensaje para Discord..."></textarea>
                </div>
                <div class="form-group">
                    <label>Canal (opcional)</label>
                    <input type="text" id="discordChannel" placeholder="#anuncios">
                </div>
                <button onclick="sendToDiscord()">Enviar a Discord</button>
                <p class="success" id="discordSuccess">✅ Mensaje enviado a Discord</p>
            </div>

            <!-- ESTADÍSTICAS -->
            <div class="admin-section">
                <h2>📊 Estadísticas</h2>
                <div class="stats">
                    <div class="stat-box">
                        <p>Anuncios Publicados</p>
                        <h3 id="statAnnouncements">0</h3>
                    </div>
                    <div class="stat-box">
                        <p>Mensajes a Discord</p>
                        <h3 id="statDiscord">0</h3>
                    </div>
                </div>
                <p style="margin-top: 15px;">🕐 Última actividad: <strong id="lastActivity">Nunca</strong></p>
            </div>
        </div>
    </div>

    <script>
        const USER = 'mineboat';
        const PASS = '67';

        let data = {
            announcements: [],
            stats: { announcements: 0, discord: 0, lastActivity: 'Nunca' }
        };

        function loadData() {
            const saved = localStorage.getItem('mineboatData');
            if (saved) {
                data = JSON.parse(saved);
            }
        }

        function saveData() {
            localStorage.setItem('mineboatData', JSON.stringify(data));
        }

        function doLogin() {
            const user = document.getElementById('loginUser').value.trim();
            const pass = document.getElementById('loginPass').value;
            
            if (user === USER && pass === PASS) {
                document.getElementById('loginSection').classList.add('hidden');
                document.getElementById('adminSection').classList.remove('hidden');
                loadData();
                updateUI();
            } else {
                const error = document.getElementById('loginError');
                error.style.display = 'block';
                setTimeout(() => error.style.display = 'none', 3000);
            }
        }

        function doLogout() {
            document.getElementById('loginSection').classList.remove('hidden');
            document.getElementById('adminSection').classList.add('hidden');
            document.getElementById('loginUser').value = '';
            document.getElementById('loginPass').value = '';
        }

        function publishAnnouncement() {
            const text = document.getElementById('announcementText').value.trim();
            if (!text) {
                alert('Escribe un mensaje');
                return;
            }

            const announcement = {
                id: Date.now(),
                text: text,
                date: new Date().toLocaleString('es-ES')
            };

            data.announcements.push(announcement);
            data.stats.announcements++;
            data.stats.lastActivity = new Date().toLocaleString('es-ES');
            
            saveData();
            
            document.getElementById('announcementText').value = '';
            const success = document.getElementById('announcementSuccess');
            success.style.display = 'block';
            setTimeout(() => success.style.display = 'none', 3000);
            
            updateUI();
        }

        function deleteAnnouncement(id) {
            data.announcements = data.announcements.filter(a => a.id !== id);
            saveData();
            updateUI();
        }

        function sendToDiscord() {
            const text = document.getElementById('discordText').value.trim();
            const channel = document.getElementById('discordChannel').value.trim();
            
            if (!text) {
                alert('Escribe un mensaje');
                return;
            }

            const WEBHOOK_URL = 'https://discord.com/api/webhooks/1461535930652561493/rvWf74O-Z1xgk7lyZN-IV1Nnf_FVHzQQZmT-dyHARiA0H9rAuMHcbYLuESDhMtsQ4QIE';
            
            const payload = {
                content: text,
                username: 'Mineboat Admin'
            };

            if (channel) {
                payload.content = `**${channel}**\n${text}`;
            }

            fetch(WEBHOOK_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            })
            .then(response => {
                if (response.ok) {
                    data.stats.discord++;
                    data.stats.lastActivity = new Date().toLocaleString('es-ES');
                    saveData();
                    
                    document.getElementById('discordText').value = '';
                    document.getElementById('discordChannel').value = '';
                    const success = document.getElementById('discordSuccess');
                    success.style.display = 'block';
                    setTimeout(() => success.style.display = 'none', 3000);
                    
                    updateUI();
                } else {
                    alert('Error al enviar mensaje a Discord');
                }
            })
            .catch(err => {
                alert('Error de conexión: ' + err.message);
            });
        }

        function updateUI() {
            const list = document.getElementById('announcementsList');
            if (data.announcements.length === 0) {
                list.innerHTML = '<p style="color: #999;">No hay anuncios</p>';
            } else {
                list.innerHTML = data.announcements.map(a => `
                    <div class="announcement-item">
                        <button class="delete-btn" onclick="deleteAnnouncement(${a.id})">Eliminar</button>
                        <p>${a.text}</p>
                        <div class="announcement-date">${a.date}</div>
                    </div>
                `).join('');
            }

            document.getElementById('statAnnouncements').textContent = data.stats.announcements;
            document.getElementById('statDiscord').textContent = data.stats.discord;
            document.getElementById('lastActivity').textContent = data.stats.lastActivity;
        }

        loadData();
    </script>
</body>
</html>
